spring:
  application:
    name: verification-service
  datasource:
    url: ${SPRING_DATASOURCE_URL:jdbc:postgresql://verification-db:5432/verification_db}
    username: ${SPRING_DATASOURCE_USERNAME:postgres}
    password: ${SPRING_DATASOURCE_PASSWORD:bantu007#}
    driverClassName: org.postgresql.Driver
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
  jpa:
    hibernate:
      ddl-auto: validate
    show-sql: false
    properties:
      hibernate:
        dialect: org.hibernate.dialect.PostgreSQLDialect
  flyway:
    enabled: true
    baseline-on-migrate: true
  autoconfigure:
    exclude:
      - org.springframework.boot.autoconfigure.security.servlet.UserDetailsServiceAutoConfiguration
  data:
    redis:
      host: ${REDIS_HOST:redis}
      port: ${REDIS_PORT:6379}
      password: ${REDIS_PASSWORD:}
      lettuce:
        pool:
          max-active: 20
          max-idle: 10
          min-idle: 5
  kafka:
    bootstrap-servers: ${KAFKA_BOOTSTRAP_SERVERS:kafka:9092}
    producer:
      key-serializer: org.apache.kafka.common.serialization.StringSerializer
      value-serializer: org.springframework.kafka.support.serializer.JsonSerializer
      acks: all
      retries: 3
      properties:
        enable.idempotence: true
    consumer:
      group-id: ${spring.application.name}
      key-deserializer: org.apache.kafka.common.serialization.StringDeserializer
      value-deserializer: org.springframework.kafka.support.serializer.JsonDeserializer
      auto-offset-reset: earliest
      properties:
        spring.json.trusted.packages: "com.kenyarealestate.*"

server:
  port: 8082

jwt:
  secret: ${JWT_SECRET}

services:
  property-url: ${PROPERTY_SERVICE_URL:http://property-service:8083}
  user-service-url: ${USER_SERVICE_URL:http://user-service:8081}
  internal-secret: ${INTERNAL_SECRET}
  # Hosts that a client-supplied documentUrl is allowed to point at before this service will
  # fetch it server-side (SHA-256 hashing, AI screening). Prevents SSRF: without this, a
  # malicious documentUrl could make the server fetch arbitrary internal/external resources.
  # Add the public-facing document/API host (e.g. the api-gateway domain or S3 bucket host)
  # via this env var in every real deployment.
  document-url-allowed-hosts: ${DOCUMENT_URL_ALLOWED_HOSTS:localhost,user-service}
  document-analysis-enabled: ${DOCUMENT_ANALYSIS_ENABLED:false}
  document-analysis-url: ${DOCUMENT_ANALYSIS_URL:https://api.yourdocumentai.com}
  document-analysis-key: ${DOCUMENT_ANALYSIS_KEY:placeholder}
  # Separate OpenAI-compatible chat-completions endpoint used only for single-field
  # ID number OCR extraction (DocumentAnalysisService.extractIdNumber()). Reuses
  # document-analysis-enabled as the master on/off switch, but points at a
  # different server/API shape than document-analysis-url above.
  ocr-model-url: ${OCR_MODEL_URL:http://localhost:11434/v1}
  ocr-model-name: ${OCR_MODEL_NAME:qwen3.6:latest}
  smile-identity-enabled: ${SMILE_IDENTITY_ENABLED:false}
  smile-identity-url: ${SMILE_IDENTITY_URL:https://testapi.smileidentity.com/v1}
  smile-identity-partner-id: ${SMILE_IDENTITY_PARTNER_ID:placeholder}
  smile-identity-api-key: ${SMILE_IDENTITY_API_KEY:placeholder}
  ardhisasa-enabled: ${ARDHISASA_ENABLED:false}
  ardhisasa-url: ${ARDHISASA_URL:https://api.ardhisasa.go.ke}
  ardhisasa-api-key: ${ARDHISASA_API_KEY:placeholder}

gateway:
  signing-secret: ${GATEWAY_SIGNING_SECRET}

verification:
  identity-expiry-years: 2
  max-fraud-strikes: 3
  fraud-cooldown-days: 90
  identity-auto-approve-score: 85
  biometric-hard-block: ${BIOMETRIC_HARD_BLOCK:true}

redis:
  trust-status-ttl-seconds: 300
  trust-status-prefix: "trust:"

kafka:
  topics:
    verification-events: verification-events

management:
  endpoint:
    health:
      probes:
        enabled: true
      show-details: when-authorized
  health:
    livenessstate:
      enabled: true
    readinessstate:
      enabled: true
  endpoints:
    web:
      exposure:
        include: health,info,metrics,prometheus
  metrics:
    export:
      prometheus:
        enabled: true

springdoc:
  swagger-ui:
    path: /swagger-ui.html

resilience4j:
  circuitbreaker:
    instances:
      property-service:
        registerHealthIndicator: true
        slidingWindowSize: 10
        minimumNumberOfCalls: 5
        permittedNumberOfCallsInHalfOpenState: 3
        automaticTransitionFromOpenToHalfOpenEnabled: true
        waitDurationInOpenState: 10s
        failureRateThreshold: 50