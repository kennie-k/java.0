global:
  resolve_timeout: 5m

route:
  group_by: [alertname, job]
  group_wait: 30s
  group_interval: 5m
  repeat_interval: 12h
  receiver: default

receivers:
  - name: default
    webhook_configs:
      - url: ${ALERTMANAGER_WEBHOOK_URL:-http://localhost:5001/alerts}
        send_resolved: true
